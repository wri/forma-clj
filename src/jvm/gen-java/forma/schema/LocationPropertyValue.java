/**
 * Autogenerated by Thrift
 *
 * DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
 */
package forma.schema;

import java.util.List;
import java.util.ArrayList;
import java.util.Map;
import java.util.HashMap;
import java.util.EnumMap;
import java.util.Set;
import java.util.HashSet;
import java.util.EnumSet;
import java.util.Collections;
import java.util.BitSet;
import java.nio.ByteBuffer;
import java.util.Arrays;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.apache.thrift.*;
import org.apache.thrift.async.*;
import org.apache.thrift.meta_data.*;
import org.apache.thrift.transport.*;
import org.apache.thrift.protocol.*;

public class LocationPropertyValue extends TUnion<LocationPropertyValue, LocationPropertyValue._Fields> {
  private static final TStruct STRUCT_DESC = new TStruct("LocationPropertyValue");
  private static final TField PIXEL_LOCATION_FIELD_DESC = new TField("pixelLocation", TType.STRUCT, (short)1);
  private static final TField CHUNK_LOCATION_FIELD_DESC = new TField("chunkLocation", TType.STRUCT, (short)2);

  /** The set of fields this struct contains, along with convenience methods for finding and manipulating them. */
  public enum _Fields implements TFieldIdEnum {
    PIXEL_LOCATION((short)1, "pixelLocation"),
    CHUNK_LOCATION((short)2, "chunkLocation");

    private static final Map<String, _Fields> byName = new HashMap<String, _Fields>();

    static {
      for (_Fields field : EnumSet.allOf(_Fields.class)) {
        byName.put(field.getFieldName(), field);
      }
    }

    /**
     * Find the _Fields constant that matches fieldId, or null if its not found.
     */
    public static _Fields findByThriftId(int fieldId) {
      switch(fieldId) {
        case 1: // PIXEL_LOCATION
          return PIXEL_LOCATION;
        case 2: // CHUNK_LOCATION
          return CHUNK_LOCATION;
        default:
          return null;
      }
    }

    /**
     * Find the _Fields constant that matches fieldId, throwing an exception
     * if it is not found.
     */
    public static _Fields findByThriftIdOrThrow(int fieldId) {
      _Fields fields = findByThriftId(fieldId);
      if (fields == null) throw new IllegalArgumentException("Field " + fieldId + " doesn't exist!");
      return fields;
    }

    /**
     * Find the _Fields constant that matches name, or null if its not found.
     */
    public static _Fields findByName(String name) {
      return byName.get(name);
    }

    private final short _thriftId;
    private final String _fieldName;

    _Fields(short thriftId, String fieldName) {
      _thriftId = thriftId;
      _fieldName = fieldName;
    }

    public short getThriftFieldId() {
      return _thriftId;
    }

    public String getFieldName() {
      return _fieldName;
    }
  }

  public static final Map<_Fields, FieldMetaData> metaDataMap;
  static {
    Map<_Fields, FieldMetaData> tmpMap = new EnumMap<_Fields, FieldMetaData>(_Fields.class);
    tmpMap.put(_Fields.PIXEL_LOCATION, new FieldMetaData("pixelLocation", TFieldRequirementType.DEFAULT, 
        new StructMetaData(TType.STRUCT, ModisPixelLocation.class)));
    tmpMap.put(_Fields.CHUNK_LOCATION, new FieldMetaData("chunkLocation", TFieldRequirementType.DEFAULT, 
        new StructMetaData(TType.STRUCT, ModisChunkLocation.class)));
    metaDataMap = Collections.unmodifiableMap(tmpMap);
    FieldMetaData.addStructMetaDataMap(LocationPropertyValue.class, metaDataMap);
  }

  public LocationPropertyValue() {
    super();
  }

  public LocationPropertyValue(_Fields setField, Object value) {
    super(setField, value);
  }

  public LocationPropertyValue(LocationPropertyValue other) {
    super(other);
  }
  public LocationPropertyValue deepCopy() {
    return new LocationPropertyValue(this);
  }

  public static LocationPropertyValue pixelLocation(ModisPixelLocation value) {
    LocationPropertyValue x = new LocationPropertyValue();
    x.setPixelLocation(value);
    return x;
  }

  public static LocationPropertyValue chunkLocation(ModisChunkLocation value) {
    LocationPropertyValue x = new LocationPropertyValue();
    x.setChunkLocation(value);
    return x;
  }


  @Override
  protected void checkType(_Fields setField, Object value) throws ClassCastException {
    switch (setField) {
      case PIXEL_LOCATION:
        if (value instanceof ModisPixelLocation) {
          break;
        }
        throw new ClassCastException("Was expecting value of type ModisPixelLocation for field 'pixelLocation', but got " + value.getClass().getSimpleName());
      case CHUNK_LOCATION:
        if (value instanceof ModisChunkLocation) {
          break;
        }
        throw new ClassCastException("Was expecting value of type ModisChunkLocation for field 'chunkLocation', but got " + value.getClass().getSimpleName());
      default:
        throw new IllegalArgumentException("Unknown field id " + setField);
    }
  }

  @Override
  protected Object readValue(TProtocol iprot, TField field) throws TException {
    _Fields setField = _Fields.findByThriftId(field.id);
    if (setField != null) {
      switch (setField) {
        case PIXEL_LOCATION:
          if (field.type == PIXEL_LOCATION_FIELD_DESC.type) {
            ModisPixelLocation pixelLocation;
            pixelLocation = new ModisPixelLocation();
            pixelLocation.read(iprot);
            return pixelLocation;
          } else {
            TProtocolUtil.skip(iprot, field.type);
            return null;
          }
        case CHUNK_LOCATION:
          if (field.type == CHUNK_LOCATION_FIELD_DESC.type) {
            ModisChunkLocation chunkLocation;
            chunkLocation = new ModisChunkLocation();
            chunkLocation.read(iprot);
            return chunkLocation;
          } else {
            TProtocolUtil.skip(iprot, field.type);
            return null;
          }
        default:
          throw new IllegalStateException("setField wasn't null, but didn't match any of the case statements!");
      }
    } else {
      TProtocolUtil.skip(iprot, field.type);
      return null;
    }
  }

  @Override
  protected void writeValue(TProtocol oprot) throws TException {
    switch (setField_) {
      case PIXEL_LOCATION:
        ModisPixelLocation pixelLocation = (ModisPixelLocation)value_;
        pixelLocation.write(oprot);
        return;
      case CHUNK_LOCATION:
        ModisChunkLocation chunkLocation = (ModisChunkLocation)value_;
        chunkLocation.write(oprot);
        return;
      default:
        throw new IllegalStateException("Cannot write union with unknown field " + setField_);
    }
  }

  @Override
  protected TField getFieldDesc(_Fields setField) {
    switch (setField) {
      case PIXEL_LOCATION:
        return PIXEL_LOCATION_FIELD_DESC;
      case CHUNK_LOCATION:
        return CHUNK_LOCATION_FIELD_DESC;
      default:
        throw new IllegalArgumentException("Unknown field id " + setField);
    }
  }

  @Override
  protected TStruct getStructDesc() {
    return STRUCT_DESC;
  }

  @Override
  protected _Fields enumForId(short id) {
    return _Fields.findByThriftIdOrThrow(id);
  }

  public _Fields fieldForId(int fieldId) {
    return _Fields.findByThriftId(fieldId);
  }


  public ModisPixelLocation getPixelLocation() {
    if (getSetField() == _Fields.PIXEL_LOCATION) {
      return (ModisPixelLocation)getFieldValue();
    } else {
      throw new RuntimeException("Cannot get field 'pixelLocation' because union is currently set to " + getFieldDesc(getSetField()).name);
    }
  }

  public void setPixelLocation(ModisPixelLocation value) {
    if (value == null) throw new NullPointerException();
    setField_ = _Fields.PIXEL_LOCATION;
    value_ = value;
  }

  public ModisChunkLocation getChunkLocation() {
    if (getSetField() == _Fields.CHUNK_LOCATION) {
      return (ModisChunkLocation)getFieldValue();
    } else {
      throw new RuntimeException("Cannot get field 'chunkLocation' because union is currently set to " + getFieldDesc(getSetField()).name);
    }
  }

  public void setChunkLocation(ModisChunkLocation value) {
    if (value == null) throw new NullPointerException();
    setField_ = _Fields.CHUNK_LOCATION;
    value_ = value;
  }

  public boolean equals(Object other) {
    if (other instanceof LocationPropertyValue) {
      return equals((LocationPropertyValue)other);
    } else {
      return false;
    }
  }

  public boolean equals(LocationPropertyValue other) {
    return other != null && getSetField() == other.getSetField() && getFieldValue().equals(other.getFieldValue());
  }

  @Override
  public int compareTo(LocationPropertyValue other) {
    int lastComparison = TBaseHelper.compareTo(getSetField(), other.getSetField());
    if (lastComparison == 0) {
      return TBaseHelper.compareTo(getFieldValue(), other.getFieldValue());
    }
    return lastComparison;
  }


  /**
   * If you'd like this to perform more respectably, use the hashcode generator option.
   */
  @Override
  public int hashCode() {
    return 0;
  }

}
